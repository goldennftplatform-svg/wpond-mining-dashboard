[build]
  base = "dashboard"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

# Existing files in /public are served first.
# Catch-all is only for missing HTML routes — keep JSON files present in publish dir.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
