[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["echo 'No dependencies to install'"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "node distributed-processor.js"
