{
  "name": "wpond-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Local dev servers for the wPOND mining dashboard (static site; deps not needed for deploy)",
  "scripts": {
    "start": "node working-server.js",
    "serve": "node http-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
